import { PokedexNumberFormatPipe } from './pokedex-number-format.pipe';

describe('PokedexNumberFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PokedexNumberFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
